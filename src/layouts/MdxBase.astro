---
import Base from './Base.astro';
import '@/styles/demo.css';

interface Props {
	title?: string;
	lang?: string;
}

const { title, lang } = Astro.props;
---

<Base title={title} lang={lang}>
	<main class="demo demo-mdx">
		<slot />
	</main>
</Base>
